# ninjia

构建速度极快,高度并行化

# ld,lst和map

.ld（Linker Script）

作用：定义内存布局（FLASH/RAM/CCM 等）、各section（.text/.data/.bss/.\_user_heap_stack）放置地址和符号（如\_estack、\_end、heap_start）。控制链接器如何把目标文件/节映射到物理内存。
用处：确定堆/栈起止、外设映射、放置启动向量和特殊节，修改内存分配必须改这里。

.map（Linker Map）
作用：链接器输出的符号与section的地址/大小详单（每个节、每个符号的最终地址和大小、内存使用统计）。
用处：检查堆栈/堆冲突、定位大对象、确认符号地址、验证内存是否超出。调试栈/堆分布首选文件。

.lst（或 build.lst，disassembly+source 列表）
作用：ELF 反汇编并混合源代码（arm-none-eabi-objdump -d -S ...），展示机器码、汇编指令与对应源行。
用处：精确定位崩溃指令（配合 addr2line/PC），检查编译器优化影响，查看函数内具体指令和寄存器访问。

# ARM GCC 工具链

arm-none-eabi 表示“面向 ARM 架构、无操作系统、采用嵌入式 ABI”的交叉编译工具链
定义是什么：ABI（Application Binary Interface）规定二进制层面的函数调用约定、参数传递、返回值、寄存器保存、栈帧布局、对齐规则、异常/中断约定和数据类型大小/对齐，确保不同编译单元、库、汇编代码能二进制互操作

## arm-none-eabi-addr2line

ARM GCC 工具链中的一个命令行工具，用于将程序的地址（如崩溃时的 PC 地址）转换为源码文件名和行号，帮助你定位代码中的具体位置

## arm-none-eabi-objdump

arm-none-eabi-objdump -d -S ./build/build.elf > ./build/build.lst
-d = --disassemble
-S = --source
将 ELF 文件反汇编，并在汇编指令旁边显示源代码

## 定位hardfault

HardFault 只有两大类：
取指错误：指令地址不存在
访问错误：地址存在，但不能读 / 不能写 / 不能 DMA / 未对齐

```c
void HardFault_Handler(void)
{
  /* USER CODE BEGIN HardFault_IRQn 0 */
  // 定义故障状态寄存器变量
  uint32_t hfsr = SCB->HFSR;    // 硬故障总状态寄存器
  uint32_t cfsr = SCB->CFSR;    // 配置故障状态寄存器（包含Mem/Bus/Usage三类错误）
  uint32_t mmfar = SCB->MMFAR;  // 内存管理错误地址寄存器
  uint32_t bfar = SCB->BFAR;    // 总线错误地址寄存器

  // 打印HardFault标题
  MIPI_CMD_DEBUG("\r\n\r\n==================== HardFault ====================\r\n");
  MIPI_CMD_ERROR("HFSR = 0x%08X\r\n", hfsr);
  MIPI_CMD_ERROR("CFSR = 0x%08X\r\n", cfsr);

  // 拆分三类错误状态
  uint8_t mmfsr = (cfsr >> 0) & 0xFF;   // 内存管理错误状态
  uint8_t bfsr  = (cfsr >> 8) & 0xFF;   // 总线错误状态
  uint16_t ufsr = (cfsr >> 16) & 0xFFFF;// 用法错误状态

  // ==============================================
  // 内存管理错误解析（打印英文，注释中文）
  // ==============================================
  if (mmfsr != 0) {
    MIPI_CMD_ERROR("[MemManage Fault]\r\n");
    if (mmfsr & (1 << 0)) MIPI_CMD_ERROR("  IACCVIOL: Instruction access violation\r\n");
    if (mmfsr & (1 << 1)) MIPI_CMD_ERROR("  DACCVIOL: Data access violation\r\n");
    if (mmfsr & (1 << 2)) MIPI_CMD_ERROR("  MUNSTKERR: Unstacking error\r\n");
    if (mmfsr & (1 << 3)) MIPI_CMD_ERROR("  MSTKERR: Stacking error\r\n");
    if (mmfsr & (1 << 4)) MIPI_CMD_ERROR("  MLSPERR: Lazy state preservation error\r\n");
    if (mmfsr & (1 << 6)) MIPI_CMD_ERROR("  MMARVALID: MMFAR address valid\r\n");

    // 若错误地址有效，则打印出错地址
    if (mmfsr & (1 << 6)) {
      MIPI_CMD_ERROR("  Fault Address MMFAR = 0x%08X\r\n", mmfar);
    }
  }

  // ==============================================
  // 总线错误解析（打印英文，注释中文）
  // ==============================================
  if (bfsr != 0) {
    MIPI_CMD_ERROR("[BusFault]\r\n");
    if (bfsr & (1 << 0)) MIPI_CMD_ERROR("  IBUSERR: Instruction bus error(PC runaway)\r\n");
    if (bfsr & (1 << 1)) MIPI_CMD_ERROR("  PRECISERR: Precise data bus error\r\n");
    if (bfsr & (1 << 2)) MIPI_CMD_ERROR("  IMPRECISERR: Imprecise data bus error\r\n");
    if (bfsr & (1 << 3)) MIPI_CMD_ERROR("  UNSTKERR: Unstacking error\r\n");
    if (bfsr & (1 << 4)) MIPI_CMD_ERROR("  STKERR: Stacking error\r\n");
    if (bfsr & (1 << 5)) MIPI_CMD_ERROR("  LSPERR: Lazy state preservation error\r\n");
    if (bfsr & (1 << 6)) MIPI_CMD_ERROR("  BFARVALID: BFAR address valid\r\n");

    // 若错误地址有效，则打印出错地址
    if (bfsr & (1 << 6)) {
      MIPI_CMD_ERROR("  Fault Address BFAR = 0x%08X\r\n", bfar);
    }
  }

  // ==============================================
  // 用法错误解析（打印英文，注释中文）
  // ==============================================
  if (ufsr != 0) {
    MIPI_CMD_ERROR("[UsageFault]\r\n");
    if (ufsr & (1 << 0))  MIPI_CMD_ERROR("  UNDEFINSTR: Undefined instruction\r\n");
    if (ufsr & (1 << 1))  MIPI_CMD_ERROR("  INVSTATE: Invalid state\r\n");
    if (ufsr & (1 << 2))  MIPI_CMD_ERROR("  INVPC: Invalid EXC_RETURN\r\n");
    if (ufsr & (1 << 3))  MIPI_CMD_ERROR("  NOCP: No coprocessor\r\n");
    if (ufsr & (1 << 8))  MIPI_CMD_ERROR("  UNALIGNED: Unaligned access\r\n");
    if (ufsr & (1 << 9))  MIPI_CMD_ERROR("  DIVBYZERO: Divide by zero\r\n");
  }

  // ==============================================
  // 硬故障总标志解析
  // ==============================================
  if (hfsr & (1 << 1))  MIPI_CMD_ERROR("  VECTBL: Vector table read fault\r\n");
  if (hfsr & (1 << 30)) MIPI_CMD_ERROR("  FORCED: Forced HardFault\r\n");
  if (hfsr & (1 << 31)) MIPI_CMD_ERROR("  DEBUGEVT: Debug event\r\n");

  // ==============================================
  // 获取当前栈指针（MSP/PSP自动判断）
  // ==============================================
  uint32_t *sp;
  __ASM volatile(
      "TST lr, #4 \n"      // 判断LR的bit2，确定使用MSP还是PSP
      "ITE EQ \n"
      "MRSEQ %0, MSP \n"   // 等于0时使用MSP
      "MRSNE %0, PSP \n"   // 等于1时使用PSP
      : "=r"(sp)
  );

  // 打印触发HardFault的PC指针（栈中偏移6*4位置）
  MIPI_CMD_ERROR("\r\n>>>>>> Fault PC = 0x%08X <<<<<<\r\n", sp[6]);
  MIPI_CMD_ERROR("=====================================================\r\n");

  /* USER CODE END HardFault_IRQn 0 */
  while (1);
}
```

1)直接定位到源码行
arm-none-eabi-addr2line -e ./build/build.elf -f -C 0x8003f8c
-e <file>：指定要查询的可执行/ELF 文件（符号信息来自该文件），等同 --exe=FILE。
-f：同时输出每个地址对应的函数名（先函数名再文件:行）。
-C：对 C++ 符号进行 demangle（把修饰名还原为可读的函数签名）2)优化导致符号消失
如果输出是 ??:0，说明该地址没有符号信息，可能是启动代码、汇编、或者没有打开调试信息（编译时要加 -g）
编译器优化（如 -O2、-Os）可能会合并、内联、删除一些函数，导致 map 文件中没有你期望的符号。
解决办法
降低优化等级，如用 -O0 重新编译，生成更详细的 map 文件。
加上调试信息，如 -g 选项

### 定位堆栈溢出

从map文件中找到堆栈范围对比SP指针的值,注意区分RTOS堆栈和裸机堆栈,裸机堆栈在ld定义
ARM Cortex‑M 内核强制规定：栈 = 满减栈（Full Descending）
压栈 PUSH：SP 先减 4，再存数据
出栈 POP：先取数据，SP 再加 4
已知：

\_estack = 0x20020000（RAM 末端，栈顶）
\_Min_Stack_Size = 0x1000（4096 字节）
\_Min_Heap_Size = 0x200（512 字节）
栈（向下增长）

栈区（按最小栈大小）：
起始地址（栈底）= 0x20020000 - 0x1000 = 0x2001F000
结束地址（栈顶）= 0x20020000
区间：0x2001F000 — 0x20020000
堆（向上增长）

仅给出最小堆大小 0x200，但堆的起始地址由链接器符号（通常为 \_end 或 \_\_heap_start，即 .bss/.data 末尾）决定，未在你提供的信息中出现。
如果“假设”把最小堆放在栈下方紧邻栈底，则（仅为假设）：
堆区（最小） = 0x2001EE00 — 0x2001F000 （0x200 大小）

### 数组越界

# 工具链

## ARM Cortex‑M编译

ARM GCC
<!-- D:\Software_Install_Path\STM32CubeIDE_1.18.1\STM32CubeIDE\plugins\com.st.stm32cube.ide.mcu.externaltools.gnu-tools-for-stm32.13.3.rel1.win32_1.0.0.202411081344\tools\bin -->
cmake
<!-- C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin -->
cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=arm-none-eabi-gcc -DCMAKE_CXX_COMPILER=arm-none-eabi-g++
<!-- cmake -S . -B build ... 这一步会读取源码目录下的 CMakeLists.txt 来生成 build.ninja（或其他生成器文件）。
如果你要 Release，把 Debug 改成 Release 即可。
 -->
cmake --build build -j
