# NAND FLASH分类
目前电脑大部分采用了基于NAND Flash产品的固态硬盘
![alt text](存储\image.png)
# NAND和NOR FLASH区别
1) 访问方式
NOR Flash：随机地址读取快，可像内存一样映射执行（XIP）。
NAND Flash：按页/块访问，随机读慢，顺序读写效率高。
2) 读写/擦除特性
NOR：读快、写慢、擦除慢；擦除粒度较小（扇区）。
NAND：写入/擦除更快；按页写、按块擦。
3) 容量与成本
NOR：容量通常较小，成本高/bit。
NAND：容量大很多，成本低/bit。