# MER1200G无线桥接
## 复位MER1200G 
路由器针戳复位(网口灯灭)后要等一段时间才能进后台(如melogin.cn),很不稳定,复位后过一会儿再试
# 查看主路由器私有IP地址
手机连接主路由器wifi可以看到主路由器私有IP地址,也可以查看自己的私有ip
## LAN设置
需要该路由器IP地址在桥接路由器的子网里,如主路由器私有ip为192.168.10.1,子路由器要为192.168.10.2
### 子网掩码
1为网络位 0为主机位 可用主机数 = 2^ 主机位数 − 2,网络地址（第一个 IP）,广播地址（最后一个 IP）
### DNS服务器
绝大多数情况下，确实会自动用主路由的 IP 当 DNS
主路由拿到你的 DNS 请求后，会转发到它自己设置的上游 DNS（比如 223.5.5.5、114.114.114.114）
### 网关
为什么桥接模式就不能当网关？
网关必须干 3 件事：
分配 IP（DHCP）
转发数据包（NAT）
提供出口（Gateway）
### ip知识
10.x → 超大内网
172.16～31.x → 中等内网
192.168.x → 家庭小内网
其他所有 IP → 都是公网

## 关闭子路由器的DHCP服务
1. 避免 IP 地址冲突（两虎相争）
主路由器和子路由器默认都会开启 DHCP 服务，自动给连接的设备分配 IP。
如果都开启，它们可能会同时给两个不同的设备分配同一个 IP（例如都分了 192.168.1.10），导致网络掉线或频繁冲突。
2. 统一网关，确保能上网
子路由器的 DHCP 通常会将自己设为“默认网关”。但子路由器本质上只是个中转站，它自己并不直接连接宽带。
如果鲁班猫接在子路由上并获取了子路由发的 IP 和网关，它的流量会卡在子路由里，出不去公网。
关闭子 DHCP 后：鲁班猫会跨过子路由，直接向主路由器申请 IP。此时，主路由会告诉鲁班猫：“我是网关，跟我走能上网”。

# 端口转发(虚拟服务器2002220022)1

你的鲁班猫 IP 是 192.168.223.238（局域网私有 IP），而 116.229.107.251 是路由器的。

问题： 当你在外面访问 116.229.107.251 时，路由器不知道要把这个请求发给哪台电脑。
解决方法： 你需要在路由器后台设置 “端口转发 (Port Forwarding)”：
将外部端口 22 (SFTP) 或 21 (FTP) 转发到内部 IP (鲁班猫) 的对应端口。

对于 **水星 (MERCURY) MER1200** 路由器，设置端口转发（官方术语通常称为“**虚拟服务器**”）的具体步骤如下：

### 1. 登录路由器后台
*   打开浏览器，输入路由器的管理地址（通常是 `192.168.1.1` 或 `melogin.cn`）。
*   输入管理员密码登录。

### 2. 找到“虚拟服务器”设置
在菜单栏中依次点击：
1.  **高级功能** 
2.  **虚拟服务器** 

### 3. 添加转发规则
点击“**添加**”按钮，填写以下信息：

| 项目 | 设置内容 | 说明 |
| :--- | :--- | :--- |
| **服务器名称** | `LubanCat_SFTP` | 随便起个名字 |
| **外部端口** | `20022` | **强烈建议使用非22端口**（防止暴力破解和运营商屏蔽） |
| **内部端口** | `22` | 鲁班猫默认的 SSH/SFTP 端口 |
| **内部服务器IP** | `192.168.1.109` | 你的鲁班猫 IP |
| **协议类型** | `TCP` | SFTP 基于 TCP |

*   *注意：如果你要用传统的 FTP（不推荐，不安全且配置复杂），内部端口填 `21`。*

### 4. 保存并生效
点击“**保存**”或“**确定**”。

---

### 5. FileZilla 连接参数
设置完成后，你在 **外网（如咖啡店、手机热点）** 连接时，FileZilla 的设置如下：

*   **协议**：SFTP - SSH File Transfer Protocol
*   **主机 (Host)**：`116.229.107.251`
*   **端口 (Port)**：`20022` (即你在“外部端口”填的那个)
*   **用户/密码**：你的鲁班猫账号密码（通常是 `cat` / `temppwd`）

### ⚠️ 两个致命故障点检查：

1.  **IP 段不匹配问题：**
    你之前的 `ifconfig` 显示鲁班猫 IP 是 `192.168.223.238`。
    **重点：** 如果你的 MER1200 路由器后台显示的网段是 `192.168.1.x`，那么说明鲁班猫**并不是直接插在路由器上**的，而是插在另一台设备（比如电脑或另一个路由器）上。
    *   **解决：** 鲁班猫必须直接插在 MER1200 的 LAN 口，或者确保它的 IP 是由 MER1200 分配的，端口转发才会生效。

2.  **公网 IP 检查（必做）：**
    在路由器后台首页，查看“**运行状态**”或“**WAN口状态**”。
    *   如果后台显示的 **WAN口 IP** 与 `116.229.107.251` **不一致**，说明你没有真正的公网 IP，端口转发将无法从外网访问。